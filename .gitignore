# HAR captures of the target site (may contain session/auth data) — never commit these.
*.har

# .NET build output
bin/
obj/

# Nix build output symlinks
result
result-*

# Local env/secrets
.env
*.local.json
